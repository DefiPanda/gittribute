<h1>Listing comments</h1>

<table>
  <tr>
    <th>Content</th>
    <th>Repoid</th>
    <th>Userid</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.content %></td>
    <td><%= comment.repoid %></td>
    <td><%= comment.userid %></td>
    <td><%= link_to 'Show', comment %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comment', new_comment_path %>
